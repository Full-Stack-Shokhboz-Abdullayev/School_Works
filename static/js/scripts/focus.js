// const links = document.querySelectorAll(".dropdown li");
// const angles = document.querySelectorAll(".angle");

// angles.forEach((angle, i) => {
// 	angle.addEventListener("click", (_) => {
// 		document.querySelector("#d" + (i + 1)).classList.add("focused");
// 	});
	
// });

// links.forEach((link, i) => {
// 	link.addEventListener("click", () => {
// 		document.querySelector("#d" + (i + 1)).classList.add("focused");
// 	});
// 	link.addEventListener("blur", () => {
// 		document.querySelector("#d" + (i + 1)).classList.remove("focused");
// 	});
// });
